{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }} text-center shadow mb-4">
            {{ message|raw }}
        </div>
    {% endfor %}
{% endfor %}