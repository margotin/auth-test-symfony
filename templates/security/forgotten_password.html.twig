{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-6 col-xl-4 mt-5">
            {{ form_start(form) }}
            <div class="card shadow-lg">
                <div class="card-header">
                    <h3 class="mb-0 text-center">
                        Mot de passe oublié ?
                    </h3>
                </div>
                <div class="card-body">
                    {{ form_widget(form) }}
                </div>
                <div class="card-footer bg-transparent">
                    <button type="submit" class="btn btn-block btn-primary">Envoyer</button>
                    <a href="{{ path('security_login') }}" class="btn btn-block btn-outline-info">Retour</a>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}